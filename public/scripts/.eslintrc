# ESLint overrides for public facing JavaScript files utilized within HTML
rules:

  # Silence undefined usage error since variables and functions can be used across files
  no-undef:
    - off

  # Silence unused variable error since variables and functions can be used across files
  no-unused-vars:
    - off
